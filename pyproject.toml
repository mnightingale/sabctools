[build-system]
# cmake and ninja build the vendored aws-lc. Taking them from PyPI keeps the build
# self-contained on every platform, instead of relying on the system package manager.
requires = ["setuptools>=77.0.0", "wheel", "cmake>=3.20", "ninja"]
build-backend = "setuptools.build_meta"

[project]
name = "sabctools"
dynamic = ["version"]
description = "C implementations of functions for use within SABnzbd"
readme = "README.md"
authors = [
    {name = "Safihre", email = "safihre@sabnzbd.org"},
]
requires-python = ">=3.10"
license = "GPL-2.0-or-later"
license-files = ["LICENSE.md"]
classifiers = [
    "Programming Language :: Python",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: C++",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: Unix",
    "Operating System :: MacOS",
    "Development Status :: 5 - Production/Stable",
    "Environment :: Plugins",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Communications :: Usenet News",
]

[project.urls]
Homepage = "https://github.com/sabnzbd/sabctools/"
Source = "https://github.com/sabnzbd/sabctools/"
Issues = "https://github.com/sabnzbd/sabctools/issues"

[dependency-groups]
test = [
    "pytest",
    "chardet",
    "jaraco.functools",
    "portend"
]

[tool.black]
line-length = 120
target-version = ['py310']
include = '(?:src|tests)\/.*\.pyi?'

[tool.cibuildwheel]
skip = "*i686* *t-*"
test-skip = "*"

[tool.cibuildwheel.linux]
archs = ["native"]

[tool.cibuildwheel.macos]
archs = ["universal2"]
build = "cp314-*"

[tool.cibuildwheel.windows]
archs = ["native"]
build = "cp314-*"
# Skip wheel repair: SABnzbd already bundles the required runtime DLLs
repair-wheel-command = ""
# aws-lc needs NASM for the x86_64 assembly, without it the build falls back to
# the (slower) pure C implementations
before-all = "choco install nasm -y --no-progress || exit 0"

[tool.setuptools]
packages = ["sabctools"]
package-dir = {"sabctools" = "src"}
include-package-data = false
